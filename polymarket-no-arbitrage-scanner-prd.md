# 需求文档：Polymarket 分区标的 NO 不败套利扫描器

## 1. 背景与目标

搭建一个针对 Polymarket **分区型标的** 的订单簿扫描工具，自动识别 **NO 不败** 套利机会，并输出可执行的等额买入方案：

- **NO 不败**：买入一组连续档位的 NO，无论结算落在哪里都保证不亏（无条件不败）。核心思路是利用市场滞后：多档标的的 NO 卖一价之和通常接近但偶有偏离平衡点，当出现"局部档位 NO 卖一价和 ≤ k−1"时，存在无风险窗口。

本工具只关注 NO 侧套利，不涉及 YES 侧交易。

## 2. 适用范围（标的类型）

只处理满足"分区结构"的标的：**多个互斥档位，恰好一档结算为 $1，其余为 $0**。

### 2.1 扫描范围：weather 全部天气市场

扫描器统一使用 Polymarket 的 **`weather` tag** 聚合 weather 主页（`https://polymarket.com/zh/weather`）上**所有天气相关市场**，包括但不限于：

- **温度档位**：最高温度 / 最低温度落在哪个区间（含 t+0 / t+1 / t+2 各交易日）
- **降水**：降水量落在哪个区间
- **飓风 / 台风**：强度、数量、是否登陆等
- **龙卷风**：数量的数量档位
- **地震**：震级数量、发生地点
- **全球 / 气候**：全球升温、北极海冰范围、年度排名等
- **传染病 / 大流行**：流感、麻疹、病例数等

> 经济数据档位（CPI、非农、GDP、利率等）作为后续扩展预留，当前阶段不扫描。

### 2.2 全量扫描与"不提前过滤"原则（重要）

**必须全量扫描，禁止提前按标签形态过滤**：

- 发现阶段拉取 `weather` tag 下**所有**未结算、未过期事件，不做任何档位数 / 标签形态的提前剔除。
- 每个事件内**所有档位（markets）全部纳入**，逐档取 NO 卖一价后再判定；档位标签能否解析成数值**只影响排序，不影响是否扫描**。
- **二元盘（YES/NO 两档，即单档事件）在扫描阶段发现后直接抛弃**，而不是在发现阶段被提前过滤掉。
- 市场是**动态变化**的，每次扫描必须实时拉取最新全量，不因标签形态或历史缓存漏掉可能出现倒挂的新市场。

明确排除（在扫描阶段判定后抛弃）：二元盘（YES/NO 两档）、独立非互斥分区事件。

## 3. 数据源与接口（硬性约束）

- **只用 Polymarket 官方 Gamma API** 获取市场数据，不引入外部行情源。
- 关键端点：
  - `GET https://gamma-api.polymarket.com/events?tag_slug=weather&closed=false&limit=100&offset={n}` → 发现事件列表。**必须用 `offset` 分页拉全**，因为 Gamma 单次返回有上限（实测约 100 条），不翻页会漏掉同一 tag 下排在后页的市场（如 `daily-temperature` 下纽约/芝加哥/米兰等 8/14 盘）。
  - `GET https://gamma-api.polymarket.com/events/{event_id}` → 返回该事件下所有市场，含 `groupItemTitle`（档位标签）、`bestAsk`（卖一价）、`bestBid`（买一价）、`outcomePrices`。
- **价格一律取自订单簿的 `bestAsk`，且只取 NO 档位市场的 `bestAsk`**。不使用 `outcomePrices` 隐含概率，也不从 YES 侧推导 NO 价格。以 `bestAsk` 作为成交价可保证按该价真实吃到单。
- 模型公平价（可选增强）可用 Open-Meteo（温度）或 consensus（经济）作为独立信号，但仅用于排序，不用于不败判定。

## 4. 核心数学模型

### 4.1 通用前提

对一类标的，市场有 n 个档位，按自然顺序排列（温度升序 / 经济区间升序）。每个档位 i 只需取 NO 侧的订单簿卖一价：

- `no_ask_i` = 该档 **NO** 卖一价（该档 NO 市场的 `bestAsk`）

只关注 NO 侧，不读取 YES 价格，也不做任何 `1 − yes_bid` 之类的概率换算。**同一结果下 YES 与 NO 的卖一价可以不为 100¢，因此不能用 YES 侧推导 NO 价格**，必须直接读取 NO 档位的 `bestAsk`，以保证按该价可以真实吃到单。

选中一个**连续子块**，含 k 个档位（2 ≤ k ≤ max_range，默认 max_range=5）。**k 是实际买入的档位数，不一定等于市场总档数 n**。

> **单位声明（必须统一，否则判定永远失败）**：订单簿 `bestAsk` 以**美分 (¢)** 计价，单档价格通常为 99、90、75 等两位数整值；而回款金额以**美元 ($)** 计价，门槛 `k−1` 是美元数。**计算与比较前必须换算成同一单位**，统一采用美分：`门槛 = (k−1) × 100 ¢`，`bestAsk` 直接以 ¢ 计。例如 k=4 档、门槛 3 美元 = 300¢，需 `Σno ≤ 300¢` 才成立；若误把 `Σno ≤ 3` 当作美分比较，任何组合都不可能满足。ROI 的计算中分子 `(k−1 − Σno)` 与分母 `Σno` 也须同为美分，单位相消后 ROI 无量纲。

### 4.2 NO 不败模型（无条件不败，子块版本）

- **不要求买满全部档**。对任意连续子块 k 档（2 ≤ k ≤ max_range）买 NO，回款门槛恒为 **k−1 美元 = (k−1)×100 ¢**。
- 条件：**Σ(no_ask_i) ≤ (k−1)×100**（单位均为美分，即块内 NO 卖一价之和 ≤ 门槛）。判定只依赖 NO 侧 `bestAsk`，不涉及任何概率或 YES 价格。
- 结算落在块内：恰 1 档 NO=0，其余 k−1 档 NO=1，每股回款 **k−1**。
- 结算落在块外：k 档全为 NO=1，每股回款 **k**。
- 因 k−1 ≥ Σno，最坏情况不亏；落在外则多赚。**不依赖结算落在哪里，是真不败。**
- 保证 ROI（最坏情形）= (k−1 − Σno) / Σno。
- **门槛 k−1 随实际买入档数 k 变化**：k 越大门槛越高，机会判定越宽松。买全部档（k=n）只是特例，是最保守的兜底版本，且天然被本子块扫描覆盖。
- NO 侧不需要峰值档约束。

### 4.3 买入方案（固定每档 10 份）

对选中块内每个档位**固定买入相同份数 N = 10 份**，不按预算等额分配：

- cost/份 = Σno，总投入 = Σno × N。
- 最坏回款（块内结算）= k−1 × N，最坏净赚 = (k−1 − Σno) × N。
- 块外结算回款 = k × N，块外净赚 = (k − Σno) × N。

输出每档的：档位、价格、买入份数（=10）、投入金额、命中回款、盈亏。

## 5. 扫描算法

对每个事件，取回全部档位后：

1. **全量发现**：分页拉全 `weather` tag 下所有未结算、未过期事件，**不做任何提前过滤**（不按档位数、不按标签形态剔除）。
2. **全档纳入**：事件内所有档位（markets）全部保留，逐档读取 NO 侧 `bestAsk`，得到 `no_ask` 序列。档位标签解析成数值上下界（`lo, hi`）仅用于排序；解析失败的档位保持原顺序，**不因解析失败而被剔除**。
3. **二元盘抛弃**：单档事件（YES/NO 二元盘）在扫描阶段发现后直接跳过，不参与套利判定。
4. 枚举所有连续子块 `[s, e]`，要求 `2 ≤ e−s+1 ≤ max_range`（默认 5）。
5. 对每个子块计算 NO 侧条件（单位统一为美分）：
   - `Σno`、是否 `≤ (k−1)×100`（k=子块档数）、ROI（最坏情形）。
6. 过滤出 `Σno ≤ (k−1)×100` 的子块，按最坏 ROI 降序排序，每个标的取 ROI 最高的一种组合，输出其每档固定买入 10 份的明细。
7. 允许配置 NO 侧的 max_range，以适应不同标的的档位密度。

### 5.1 组合选择规则（每个标的只选一种）

对每个事件，枚举全部满足 `Σno ≤ (k−1)×100` 的连续子块（含全仓 k=n），**每个标的只选用一种"回报最高"的组合下单**，不做并行叠加：

- 回报排名：以**最坏情形 ROI = ((k−1)×100 − Σno) / Σno** 为唯一排序键（分子分母均以美分计，ROI 无量纲），ROI 最高者为该标的的首选方案。
- 若多个子块 ROI 相同，优先覆盖档数多（更接近全仓）者，降低结果敏感性。
- 此规则同时自然覆盖全仓：全仓 k=n 也是被枚举的子块之一，只有在全仓 ROI 最高时才被选中。
- 输出该标的：所选子块档位范围、各档 NO 价格、Σno 与门槛 k−1、最坏 ROI、每档固定买入 10 份的方案明细。

## 6. 输出格式

扫描器输出应包含以下信息：

- 事件名称与 slug
- 选中的连续子块（档位范围）
- 各档位 NO 价格明细
- Σno（美分）与门槛 (k−1)×100 美分的对比
- 最坏情形 ROI
- 买入方案（每档固定买入 10 份、投入金额、命中回款）
- 回款情景分析（块内结算 vs 块外结算）